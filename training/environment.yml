name: training-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - jupyter
  - pandas
  - numpy
  - scipy
  - pywavelets  # PyWavelets is installed with the package name 'pywavelets', not 'pywt'
  - pip
  - scikit-learn
  - matplotlib
  - sktime # for rocket
  - pyts # for rocket
  - pip:
    - git+https://github.com/gon-uri/detach_rocket#egg=detach_rocket # for rocket
